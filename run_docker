docker run -d -v "$(pwd)/data:/data" --network host --name frontend frontend
docker run -d -v "$(pwd)/data:/data" --network host --name backend backend
docker run -d -v "$(pwd)/data:/data" --network host --name ros-prod ros-prod
